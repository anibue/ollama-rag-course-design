```cmd
curl http://localhost:8090/api/tags
```

端口映射在docker desktop（win）8090:11434

Python环境：使用conda包管理，name：nlprag

```cmd
conda activate nlprag
```

scikit-learn langchain-ollama

```cmd
pip freeze > requirements.txt
```

